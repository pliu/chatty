go/ contains the Go server code
android/ contains the Android client code
ios/ contains the iOS client code
All of the clients should have the same functionality, supported by the singular Go backend
Ignore TODOs in the code
Don't execute git commands
